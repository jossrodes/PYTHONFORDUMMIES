price = raw_input('Enter price: ')
qty = raw_input('Enter quantity: ')
price * qty
price, type(price)
qty, type(qty)
price = float(price)
qty = int(qty)
price * qty
price = float(raw_input('Enter price: '))
